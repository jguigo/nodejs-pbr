module.exports.area_quadrado = (lado) => lado * lado;
module.exports.area_retangulo = (base, altura) => base * altura;
module.exports.area_triangulo = (base, altura) => (base * altura) / 2;
module.exports.area_circulo = (raio) => Math.PI * raio * raio;